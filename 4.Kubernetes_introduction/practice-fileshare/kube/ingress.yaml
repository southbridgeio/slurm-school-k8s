---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: fileshare
spec:
  rules:
  - host: s<номер своего логина>.k8s.slurm.io
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: fileshare
            port: 
              number: 80
      
